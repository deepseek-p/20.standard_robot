# 调参记录: pitch_adaptive_ff

日期: 2026-02-27

---

## 17:15:09 | 满仓向下限运动迟钝：PID输出不足以克服重力，前馈未完全收敛

### 参数变更

**调前:**
```
angle Kp=24 Ki=0 max_out=10.0, speed Kp=800 Ki=10 max_out=15000 max_iout=3000, FF: GAMMA=0.005, K_MAX=15000, B_MAX=10000, SPEED_TH=0.5, SAT_TH=15000, ALPHA_DOT_MAX=50
```

**调后:**
```
待修改
```

### 效果对比

满仓摇杆向下拉满(rc_ch3=-660): pitch_rel=0.11 vs pitch_rel_set=0.19, 稳态误差0.08rad, pitch_cur=10721已稳定不再变化(std=140)。推算: 角度误差0.08*Kp24=1.92rad/s速度设定, 速度环I_pid≈800*1.92+积分≈4536, I_ff≈6000, 总计≈10536与实测10721吻合。PID输出被角度环小误差限制，无法提供更大推力。

### 证据文件

- [data/tune_2026-02-27_030.csv](../../data/tune_2026-02-27_030.csv)
- [data/tune_2026-02-27_031.csv](../../data/tune_2026-02-27_031.csv)

### 结论

根本原因：角度环Ki=0无积分项，小角度误差时PID输出不够大。前馈虽同方向帮忙但未完全收敛到精确值。修复方向：1)前馈与PID解耦，对总电流做±16000硬限幅而非依赖PID的max_out; 2)遥测增加K_hat/b_hat/I_ff输出便于观测前馈收敛状态; 3)考虑角度环加小Ki消除稳态误差。

---

## 18:00:40 | 满仓动态响应测试：逆重力方向1-1.5秒到达目标，顺重力方向0.5秒，不对称明显

### 参数变更

**调前:**
```
GAMMA_K=0.005, GAMMA_B=0.005, ALPHA_DOT_MAX=50, 硬限幅16000(#7c新增)
```

**调后:**
```
待修改：需加快GAMMA收敛速度
```

### 效果对比

15秒连续操作摇杆上下拉满。逆重力(rc=-660): idx25→75约1秒追到目标，峰值电流14836；顺重力(rc=+660): idx100→125约0.5秒到达，电流6972。前馈收敛后响应变快，但初始阶段迟钝。

### 证据文件

- [data/tune_2026-02-27_032.csv](../../data/tune_2026-02-27_032.csv)
- [data/tune_2026-02-27_033.csv](../../data/tune_2026-02-27_033.csv)
- [data/tune_2026-02-27_034.csv](../../data/tune_2026-02-27_034.csv)

### 结论

硬限幅16000解耦有效（电流能到14836），但GAMMA=0.005收敛太慢，满仓切换方向时前馈需要1秒以上适应。下一步：增大GAMMA或用误差自适应GAMMA加快收敛。

---

## 18:16:17 | 误差自适应GAMMA被rate limit截断，K_hat≈b_hat模型退化为单参数

### 参数变更

**调前:**
```
GAMMA_BASE=0.005, GAMMA_ERR_GAIN=0.5, GAMMA_MAX=0.05, ALPHA_DOT_MAX=50, K_MAX=15000, B_MAX=10000
```

**调后:**
```
待修改：简化模型+提高rate limit
```

### 效果对比

15秒连续操作数据：K_hat和b_hat始终几乎相等(3102→3446)，缓慢上升未收敛。逆重力方向err=0.32→0.09→0.02约1秒追到，与#7c无明显改善。电流峰值15081接近硬限幅16000。GAMMA自适应被ALPHA_DOT_MAX=50截断，实际单步max_step=0.05，大GAMMA无法发挥。

### 证据文件

- [data/tune_2026-02-27_035.csv](../../data/tune_2026-02-27_035.csv)

### 结论

自适应GAMMA效果不明显，原因是ALPHA_DOT_MAX=50的rate limit截断了大GAMMA的更新步长。另外K_hat≈b_hat始终相等，cos(θ)项无区分作用（θ范围±0.2rad, cos≈0.98≈1），模型退化为单参数。下一步：1)去掉K*cos(θ)项只保留b_hat简化模型; 2)提高ALPHA_DOT_MAX或去掉rate limit让自适应GAMMA真正生效。

---

## 18:48:07 | Codex#7e全场景验证通过：满仓/发射中/空仓均正常，自适应前馈实时跟踪弹量变化

### 参数变更

**调前:**
```
GAMMA_BASE=0.005, GAMMA_ERR_GAIN=0.5, GAMMA_MAX=0.05, K_MAX=15000, B_MAX=10000, SAT_TH=15000, ALPHA_DOT_MAX=50
```

**调后:**
```
GAMMA_BASE=0.005, GAMMA_ERR_GAIN=0.5, GAMMA_MAX=0.05, B_INIT=0, B_MAX=15000, SPEED_TH=0.5, ALPHA_DOT_MAX=500, 硬限幅16000, 无SAT_TH, 单参数b_hat(去掉K*cos项)
```

### 效果对比

满仓动态: 逆重力0.6秒追到,err≤0.06rad,b_hat≈8500,cur峰值15000。发射中: b_hat从7800→5900实时跟踪弹量下降,err≤0.06。空仓静态: b_hat≈-96,cur≈-99,err≈0。空仓动态: 逆重力0.6秒追到,err≤0.12,b_hat≈800,cur峰值4000。

### 证据文件

- [data/tune_2026-02-27_036.csv](../../data/tune_2026-02-27_036.csv)
- [data/tune_2026-02-27_037.csv](../../data/tune_2026-02-27_037.csv)
- [data/tune_2026-02-27_038.csv](../../data/tune_2026-02-27_038.csv)
- [data/tune_2026-02-27_039.csv](../../data/tune_2026-02-27_039.csv)

### 结论

Codex#7e 全场景验证通过。简化为单参数b_hat + 去掉SAT_TH + ALPHA_DOT_MAX=500，解决了所有之前的死锁和迟钝问题。Pitch自适应重力前馈调参完成，可进入下一阶段。

