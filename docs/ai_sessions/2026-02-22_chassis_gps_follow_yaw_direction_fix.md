# 2026-02-22_chassis_gps_follow_yaw_direction_fix

## 0. 根因路径三问（按 00_AI_HANDOFF_RULES 6.1）

1. 当前故障实际执行路径是什么？
- 依据你最新“纯 GPS”日志（20:20:10 段）可见：
  - `ch_mode=0`（列11，`CHASSIS_VECTOR_FOLLOW_GIMBAL_YAW`）
  - `yaw_mode=1`、`pitch_mode=1`（列23/24，绝对角）
  - `rc_ch2=0`、`rc_ch3=0`（列42/43，无 yaw/pitch 输入）
  - `wz_set` 长时间触发 `±6000`（列14，对应 `±6rad/s`），轮电流列 19~22 同步大幅冲击。
- 依据你最新“切挡位”日志（20:22:02 段）可见：ATTI(`ch_mode=2`) -> GPS(`ch_mode=0`) 后，`wz_set` 立即进入大幅摆动。
- 结论：故障持续发生在底盘“跟随云台 yaw”闭环分支，不在 `NO_FOLLOW` 分支。

2. 本次修改命中的是哪条路径？
- 命中 `application/chassis_task.c` 的 `chassis_set_contorl()` 中 `CHASSIS_VECTOR_FOLLOW_GIMBAL_YAW` 分支。

3. 如何用数据证明本次修改生效？
- 观测 USB 列：`11,14,15,16,19,20,21,22,42,43,44`。
- 期望变化：
  - `rc_s0=1` 且 `rc_ch2=rc_ch3=0` 时，`wz_set` 不再长期打满 `±6000`，应明显低于饱和并可收敛。
  - 轮电流（19~22）不再出现持续高幅同向冲击。
  - 切挡后允许短暂过渡，但不应持续振荡。

## 1. 目标

- 本次要解决的问题：GPS 跟随模式无输入时底盘自激旋转（`wz_set` 饱和、轮电流冲击）。
- 预期可观测结果：无输入静置时 `wz_set` 与轮电流明显收敛，切挡后不再持续放大。

## 2. 改动文件列表

- `application/chassis_task.c`
- `application/chassis_task.h`
- `docs/ai_sessions/2026-02-22_chassis_gps_follow_yaw_direction_fix.md`
- `docs/INDEX.md`
- `docs/risk_log.md`

## 3. 关键改动摘要

- 第 1 轮（已做，未闭环）：
  - `wz_set = -PID_calc(...)` -> `wz_set = PID_calc(...)`（修正方向符号）。
  - 你反馈“现象仍一样”，说明仅改符号不足。
- 第 2 轮（本次新增）：
  - 在跟随分支改为“先算环绕误差” `delta = rad_format(set - ref)`，避免角度差跨界带来的误判。
  - 增加小误差死区：`|delta| <= 0.03rad` 时清理角度环并输出 `wz_set=0`。
  - 增加 GPS 跟随专用限幅：`|wz_set| <= 2.5rad/s`，抑制持续满量程振荡。
  - 在底盘反馈中补齐 `chassis_relative_angle` 更新，便于后续日志对齐分析。

## 4. 关键决策与理由

- 决策 1：不再继续改符号，改为“阻尼与饱和管理”。
- 理由：你的最新日志已显示符号关系已正确（`yaw_rel` 正时 `wz_set` 为负，能把误差往 0 拉），但仍因环路过激导致反复饱和。
- 决策 2：采用“误差环绕 + 死区 + 专用限幅”的最小闭环修复。
- 理由：只针对问题路径，避免全局 PID 参数联动引入新副作用。

## 5. 实时性影响（必须写）

- 阻塞变化：无新增阻塞调用。
- CPU 影响：每周期新增 `rad_format/fabs/fp32_constrain` 常数级计算，可忽略。
- 栈影响：仅新增局部标量变量，对栈影响可忽略。
- 帧率/周期：未改任务周期与调度节拍。

## 6. 风险点

- 风险 1：若仍存在机械/惯量耦合，`2.5rad/s` 可能仍偏大，需进一步下调。
- 风险 2：死区过大会带来小角度跟随滞后。

## ⚠ 未验证假设

- 假设：当前剩余异常主要来自跟随环过激与持续饱和，而非模式映射错误。
- 未验证原因：第 2 轮代码刚完成，尚未收到你的改后日志。
- 潜在影响：若假设错误，可能只降低振荡幅度而未根治。
- 后续验证计划：你回传“第2轮改后”纯 GPS/切挡日志；若仍不闭环，按规则停止第3轮盲改，先补充“触发时刻前后 3s 连续帧 + 人为触发标注”。

## 7. 验证方式与结果

- 串口：USB FireWater 帧。
- USB：
  - 改动前：`wz_set` 多次达到 `±6000`，轮电流冲击明显。
  - 第1轮后（你 20:20:10 日志）：符号方向已修正，但仍持续饱和振荡。
  - 第2轮后：待你回传同工况日志验证。
- 示波器：本轮未使用。
- 上位机：VOFA+/串口工具。
- 长跑：本轮未执行。

## 8. 回滚方式（如何恢复）

- 代码回滚点：
  - 回退 `application/chassis_task.c` 中跟随分支误差处理与限幅逻辑。
  - 回退 `application/chassis_task.h` 中新增的两个防振宏。
- 参数回滚点：无新增运行时参数。
- 运行时保护：异常时先切回 ATTI（`rc_s0=3`）并保持摇杆中位。

## 9. 附件/证据

- 日志来源：本会话你提供的 20:20:10（纯GPS）与 20:22:02（切挡位）原始帧。
- 相关风险条目：`docs/risk_log.md` 中 2026-02-22 `chassis_task` 条目。

## 10. 第3轮（参数收敛）执行记录（2026-02-22）

### 10.1 改动前三问（按 00_AI_HANDOFF_RULES 6.1）

1. 当前故障实际执行路径是什么？
- 你新增日志（20:41:38）在纯 GPS 下显示：`ch_mode=0`、`rc_ch2=rc_ch3=0` 时 `wz_set` 仍反复触发 `±2500`（旧限幅上限），轮电流列 19~22 仍有明显振荡。
- 切挡后（20:42:54 起）先进入 `ch_mode=2`，`wz_set` 与电流明显收敛；再次回到 `ch_mode=0` 后振荡重新出现。
- 结论：问题路径仍是 `CHASSIS_VECTOR_FOLLOW_GIMBAL_YAW`，但幅度相比前一轮“没那么剧烈”。

2. 本次修改命中的是哪条路径？
- 命中同一路径，仅做参数收敛：`application/chassis_task.h` 的跟随环 `Kp/死区/wz_limit` 宏。

3. 如何用数据证明本次修改生效？
- 观测列：`11,14,15,16,19~22,42,43,44`。
- 预期：
  - 在 `ch_mode=0` 且 `rc_ch2=rc_ch3=0` 时，`wz_set` 不应再长期贴 `±1600`（新限幅）。
  - 轮电流峰值与往返频率继续下降。
  - 切挡（ATTI↔GPS）仅短暂过渡，不出现持续摆振。

### 10.2 本轮代码改动（已执行）

- `application/chassis_task.h`
  - `CHASSIS_FOLLOW_GIMBAL_PID_KP: 40.0f -> 8.0f`
  - `CHASSIS_FOLLOW_GIMBAL_WZ_LIMIT: 2.5f -> 1.6f`
  - `CHASSIS_FOLLOW_GIMBAL_DEAD_BAND: 0.03f -> 0.06f`

### 10.3 本轮实时性影响

- 无新增分支复杂度与阻塞路径，仅修改常量参数。
- 控制周期与任务调度不变。

### 10.4 本轮回滚点

- 恢复 `application/chassis_task.h` 三个宏为：
  - `Kp=40.0f`
  - `wz_limit=2.5f`
  - `dead_band=0.03f`

## ⚠ 未验证假设（第3轮）

- 假设：当前剩余振荡主因是角度环增益偏大 + 限幅偏宽，而非模式判定错误或方向错误。
- 未验证原因：第3轮参数刚写入，尚未收到改后实机日志。
- 潜在影响：若机械耦合占主导，降 `Kp` 可能只降低摆幅，不一定完全消除摆振。
- 后续验证计划：按“纯 GPS 静置 10s + 切挡 3 次”回传同列日志，优先核对 `wz_set` 是否仍长期贴限与电流是否继续同频摆动。

